# Review Journal

I treated `axiom-rel-breaker-probe` as a project where the smallest useful behavior should still be inspectable.

The local checks classify each case as `ship`, `watch`, or `hold`. That gives the project a small review vocabulary that matches its reliability focus without claiming live deployment or external usage.

## Cases

- `baseline`: `budget pressure`, score 113, lane `watch`
- `stress`: `failure width`, score 186, lane `ship`
- `edge`: `recovery gap`, score 216, lane `ship`
- `recovery`: `runbook drift`, score 209, lane `ship`
- `stale`: `budget pressure`, score 223, lane `ship`

## Note

The repository should be understandable without pretending it is larger than it is.
